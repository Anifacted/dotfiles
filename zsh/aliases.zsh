alias reload!='. ~/.zshrc'

alias cls='clear' # Good 'ol Clear Screen command

alias world='zdi services restart && zdi apps restart'

# I can accidentally type 't' before 'i'
alias gti="git"        

# I can accidentally type 'u' or 'o' which are neighbors to 'i'
alias got="git"   
alias gut="git"